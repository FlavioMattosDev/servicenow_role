---
# defaults/main.yml

# Timeout e Validação SSL padrão
servicenow_default_timeout: 30
servicenow_default_validate_certs: true

# Variáveis que controlam o fluxo (devem ser sobrescritas no playbook)
snow_entity: "" # Ex: incident, change, ritm
snow_action: "" # Ex: create, update, info, delete
